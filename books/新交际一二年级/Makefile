# 在本目录执行: make
# 配置见 book.json；新增词汇文件时请同步更新 VOCAB 与 book.json

REPO_ROOT := $(abspath $(CURDIR)/../..)
VOCAB     := 一年级词汇.md 二年级词汇.md 基础阅读400词汇.md translations.json ipa.json

include $(REPO_ROOT)/scripts/Makefile.inc
