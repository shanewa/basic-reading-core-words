# When adding patterns here, mirror them in .dockerignore so Docker builds
# do not ship those paths into the image.
*.pyc
.venv/*
books/*/*.pdf
books/*/.cache/
.claude/
# SQLite WAL/SHM ephemeral files (created by journal_mode=WAL)
*.db-wal
*.db-shm
src/data/*
proxy.env
.cursor/*
.mailmap